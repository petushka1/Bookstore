const NewBook = () => (
  <>
    <main>
      <h2>Add New Book</h2>
      <form>
        <input type="text" id="title" placeholder="add title.." />
        <input type="text" id="title" placeholder="add author.." />
        <button type="submit">ADD BOOK</button>
      </form>
    </main>
  </>
);

export default NewBook;
